export interface IAuthenticationProps {
    gridP1: number;
    autoComplete: string;
    name: string;
    id: string;
    label: string;
    type?: string;
    onChange?: any;
    helperText?: string;
}
