export enum ArticleCategory {
    MOVIES = 'MOVIES',
    SERIES = 'SERIES',
}

export enum ColorMode {
    LIGHT = 'light',
    DARK = 'dark',
}

export enum Layout {
    FLUID = 'fluid',
    FIXED = 'fixed',
}

export enum LayoutSize {
    XS = 'xs',
    SM = 'sm',
    MD = 'md',
    LG = 'lg',
    XL = 'xl',
}
